import { KfDirective } from './kf.directive';

describe('KfDirective', () => {
  it('should create an instance', () => {
    const directive = new KfDirective();
    expect(directive).toBeTruthy();
  });
});
